---
session: 8908bfeb-1816-4494-b3c9-daf3678bf701
date: 2026-03-22 23:49
source: claude-code
tags: [claude, conversation]
---

# Claude Code Session — change charlie password i used 123456 no use

## 🧑 User

change charlie password i used 123456 no use

## 🤖 Claude

Let me check your NixOS user configuration first.

---

## 🤖 Claude

没有在配置里设置 `hashedPassword`，所以密码是可变的（`mutableUsers` 默认为 `true`）。直接用 `passwd` 改就行：

---

## 🤖 Claude

密码已设置成功。用户 `charlie` 的密码现在是 `123456`。

你现在可以用这个密码登录 SDDM/KDE 了。

> **安全提醒**：`123456` 是非常弱的密码，建议之后换一个更强的密码（`passwd charlie`）。

---

