---
session: c126bdcb-2ad6-4756-b37e-969b8c3bfd8c
date: 2026-03-23 20:08
source: claude-code
tags: [claude, conversation]
---

# Claude Code Session — other account charlie i cant login.. use 

## 🧑 User

other account charlie i cant login.. use 

## 🤖 Claude

你的消息似乎不完整——"use" 后面是什么？

你是说 KDE/SDDM 里另一个用户账户 "charlie" 无法登录吗？能补充一下具体情况吗，比如：

- 是 SDDM 登录界面登录失败，还是 `su`/`sudo` 切换用户失败？
- 有什么错误提示吗？
- "use" 后面想说什么？

---

## 🤖 Claude

让我先检查一下 charlie 账户和 SDDM 的状态。

---

## 🤖 Claude

问题找到了。从日志可以看到两个问题：

1. **charlie 账户密码被锁定** — `charlie:!:` 表示密码未设置/已锁定
2. **SDDM 发送的用户名为空** `""` — 说明 SDDM greeter 没有正确传递用户名

先解决密码问题，给 charlie 设置密码：

---

## 🤖 Claude

好的，给 root 和 charlie 都设置密码 `123456`：

---

## 🤖 Claude

密码已设置成功，两个账户状态都是 `P`（有密码）。

现在 charlie 应该可以在 SDDM 登录了。注意日志里还显示 SDDM 发送的用户名是空的 `""`——确保在登录界面**手动输入用户名 `charlie`**（不要留空）。

> **提醒**：NixOS 重建后密码可能会被重置。建议在 NixOS 配置里用 `users.users.charlie.hashedPassword` 持久化密码，需要我帮你配置吗？

---

